// Simple i18n implementation for Header component
export const isRTL = false

export const translate = (key: string, options?: Record<string, any>): string => {
  // Simple translation that just returns the key for now
  // You can expand this later with actual translation logic
  return key
}
